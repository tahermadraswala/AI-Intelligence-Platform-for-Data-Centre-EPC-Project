# Copy this to `.env` at the PROJECT ROOT (one level above backend/ and
# ai_core/) — both packages read from the same file.

ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional overrides (defaults shown):
# AI_CORE_LLM_MODEL=claude-sonnet-4-6
# AI_CORE_LLM_TEMPERATURE=0.1
